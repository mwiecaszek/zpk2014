#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a;
    cin >> b;
    cin >> c;
    cout << a * b * c << endl;
    cout << 2 * a * b + 2 * a * c + 2 * b * c << endl;
}
